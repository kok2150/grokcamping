<?php
// Always put this at the very top
session_start();

// If already logged in → go back to home
if (isset($_SESSION['user_id']) || isset($_SESSION['username'])) {
    header("Location: ../index.php");  // adjust path if needed
    exit();
}

// Handle form submission
$error = '';
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $username = trim($_POST['username'] ?? '');
    $password = $_POST['password'] ?? '';

    // Very basic check (later replace with database)
    // For now — hard-coded test user (DO NOT use in real site!)
    $test_user = 'colin';
    $test_pass = 'test123';   // change this!

    if ($username === $test_user && $password === $test_pass) {
        $_SESSION['username'] = $username;
        // $_SESSION['user_id'] = 1;   // if you have DB later

        header("Location: ../index.php");
        exit();
    } else {
        $error = "Wrong username or password.";
    }
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In - GROK CAMPING</title>
    <style>
        body {
            background: #000;
            color: #fff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .login-container {
            background: #111;
            padding: 40px;
            border-radius: 12px;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 0 20px rgba(0, 255, 200, 0.2);
            border: 1px solid #00ffcc44;
        }
        h1 {
            text-align: center;
            color: #00ffcc;
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 8px;
            color: #ccc;
        }
        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #333;
            border-radius: 6px;
            background: #1a1a1a;
            color: white;
            font-size: 16px;
        }
        input:focus {
            outline: none;
            border-color: #00ffcc;
            box-shadow: 0 0 8px #00ffcc66;
        }
        button {
            width: 100%;
            padding: 14px;
            background: #00ffcc;
            color: black;
            border: none;
            border-radius: 6px;
            font-size: 18px;
            cursor: pointer;
            font-weight: bold;
        }
        button:hover {
            background: #00ccaa;
        }
        .error {
            color: #ff5555;
            text-align: center;
            margin-bottom: 15px;
        }
        .back {
            text-align: center;
            margin-top: 20px;
        }
        .back a {
            color: #00ffcc;
            text-decoration: none;
        }
    </style>
</head>
<body>

<div class="login-container">
    <h1>Sign In</h1>

    <?php if ($error): ?>
        <div class="error"><?= htmlspecialchars($error) ?></div>
    <?php endif; ?>

    <form method="post">
        <div class="form-group">
            <label for="username">Username or Email</label>
            <input type="text" id="username" name="username" required autofocus>
        </div>

        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>
        </div>

        <button type="submit">Sign In</button>
    </form>

    <div class="back">
        <p><a href="../index.php">← Back to Home</a></p>
        <p>Don't have an account? <a href="register.php">Sign Up</a></p>
    </div>
</div>

</body>
</html>